<!-- search bar -->
<div class="container search-bar">
  <div class="search-bar-outer">
    <div class="search-bar-inner">
      <form action="/search">
        <h4 class="mb-2">尋找餐廳：</h4>
        <div class="input-group">
          <input type="text" name="keyword" class="form-control" placeholder="輸入餐廳或分類" aria-label="Restaurant Name..."
            value="{{keyword}}" value="" aria-describedby="search-button">
          <div class="input-group-append">
            <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>
{{!-- add new restaurant feature --}}
<div class="container new-res d-flex flex-row-reverse">
  <a href="/restaurant/new" class="btn btn-primary mt-3">新增餐廳</a>
</div>
<!-- restaurant list -->
{{#if restaurants.length}}
<div class="container mt-3">
  <div class="row">
    <div class="col-md-12 col-12 mx-auto">
      <div class="card-columns">
        {{#each restaurants}}
        <div class="total-card">
          <a href="/restaurants/{{this._id}}" class="text-secondary">
            <div class="card mb-2">
              <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
              <div class="card-body p-3">
                <h6 class="card-title mb-1">{{this.name}}</h6>

                <div class="restaurant-category mb-1">
                  <i class="fas fa-utensils pr-2"></i> {{this.category}}
                </div>

                <span class="badge badge-pill badge-danger font-weight-normal">
                  {{this.rating}}
                  <i class="fas fa-star fa-xs"></i>
                </span>

              </div>
            </div>
          </a>
          <div class="action text-right mb-3">
            <a href="/restaurants/{{this._id}}/edit" class="border border-primary p-1">edit</a>
            <a href="/restaurants/{{this._id}}" class="border border-primary p-1">detail</a>
            <form action="/restaurants/{{this._id}}/delete" method="POST" style="display:inline">
              <button type="submit">delete</button>
            </form>
          </div>
          <!--add edit, delete, detail -->
        </div>
        {{/each}}
      </div>
    </div>
  </div>
</div>
{{!-- 如果restaurant長度為0，則跳出sweetalert --}}
{{else}}
<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/javascripts/searcherror.js"></script>
{{/if}}